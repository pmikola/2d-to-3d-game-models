# Core dependencies
torch>=2.4.0
torchvision>=0.19.0

# 3D mesh processing
trimesh>=4.0
pygltflib>=1.16
xatlas>=0.0.9

# Image preprocessing
Pillow>=10.0
rembg>=2.0
numpy>=1.24

# Texture generation (Stable Diffusion)
diffusers>=0.14.0
transformers>=4.27.4
accelerate>=0.20.0
safetensors>=0.3.0

# Model download & management
huggingface_hub>=0.20.0
einops
omegaconf
pytorch-lightning
torchdiffeq
pymeshlab

# Configuration & utilities
pyyaml>=6.0
tqdm>=4.65

# MMGP memory offloading for Hunyuan3D Paint on <24GB VRAM GPUs
mmgp>=0.9.0

# Optional: GPU acceleration (uncomment for CUDA)
# xformers>=0.0.27
# spconv-cu124>=2.3.6

# Optional: depth rendering for multi-view texturing
# pyrender>=0.1.45

# Optional: local demo UI
# gradio>=4.0
